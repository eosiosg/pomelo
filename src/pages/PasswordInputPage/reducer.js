/*
 * HomeReducer
 *
 */
const initState = {};
export default function PasswordInputPageReducer( state = initState, action ) {
    switch ( action.type ) {
        default:
            return state;
    }
}
